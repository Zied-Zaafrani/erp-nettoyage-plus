# 📊 Clients & Contracts Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                          │
│                     http://localhost:5173                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │  Clients Module  │  │ Contracts Module │  │ Other Pages  │  │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────┤  │
│  │ ClientsPage      │  │ ContractsPage    │  │ Dashboard    │  │
│  │ ClientDetailPg   │  │ ContractDetailPg │  │ Users        │  │
│  │ CreateClientPg   │  │ CreateContractPg │  │ Sites        │  │
│  │ UpdateClientPg   │  │ UpdateContractPg │  │ etc...       │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
│           │                     │                    │           │
│           └─────────────────────┼────────────────────┘           │
│                                 │                                │
│                        ┌────────▼─────────┐                     │
│                        │ React Query      │                     │
│                        │ (Caching/Sync)   │                     │
│                        └────────┬─────────┘                     │
│                                 │                                │
│                        ┌────────▼─────────┐                     │
│                        │ API Service      │                     │
│                        │ (Axios)          │                     │
│                        └────────┬─────────┘                     │
│                                 │                                │
└─────────────────────────────────┼────────────────────────────────┘
                                  │
                    ┌─────────────▼──────────────┐
                    │    REST API Gateway        │
                    │  http://localhost:3000    │
                    └─────────────┬──────────────┘
                                  │
┌─────────────────────────────────┼────────────────────────────────┐
│                     BACKEND (NestJS)                              │
│                     http://localhost:3000                         │
├─────────────────────────────────┼────────────────────────────────┤
│                                 │                                │
│  ┌──────────────────────────────▼──────────────────────────────┐│
│  │                     Controllers                             ││
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐  ││
│  │  │ ClientsController       │  │ ContractsController     │  ││
│  │  │ • POST /clients         │  │ • POST /contracts       │  ││
│  │  │ • GET /clients          │  │ • GET /contracts        │  ││
│  │  │ • GET /clients/:id      │  │ • GET /contracts/:id    │  ││
│  │  │ • PATCH /clients/:id    │  │ • PATCH /contracts/:id  │  ││
│  │  │ • DELETE /clients/:id   │  │ • DELETE /contracts/:id │  ││
│  │  │ • POST /batch           │  │ • POST /batch           │  ││
│  │  └─────────────────────────┘  └─────────────────────────┘  ││
│  └──────────────────────────────────────────────────────────────┘│
│                                 │                                │
│  ┌──────────────────────────────▼──────────────────────────────┐│
│  │                     Services                                ││
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐  ││
│  │  │ ClientsService          │  │ ContractsService        │  ││
│  │  │ • create()              │  │ • create()              │  ││
│  │  │ • findAll()             │  │ • findAll()             │  ││
│  │  │ • findById()            │  │ • findById()            │  ││
│  │  │ • update()              │  │ • update()              │  ││
│  │  │ • softDelete()          │  │ • softDelete()          │  ││
│  │  │ • restore()             │  │ • restore()             │  ││
│  │  │ • Batch operations      │  │ • Batch operations      │  ││
│  │  │ • Validations           │  │ • Validations           │  ││
│  │  └─────────────────────────┘  └─────────────────────────┘  ││
│  └──────────────────────────────────────────────────────────────┘│
│                                 │                                │
│  ┌──────────────────────────────▼──────────────────────────────┐│
│  │              Repository (TypeORM)                           ││
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐  ││
│  │  │ ClientsRepository       │  │ ContractsRepository     │  ││
│  │  │ (TypeORM Query Builder) │  │ (TypeORM Query Builder) │  ││
│  │  └─────────────────────────┘  └─────────────────────────┘  ││
│  └──────────────────────────────────────────────────────────────┘│
│                                 │                                │
└─────────────────────────────────┼────────────────────────────────┘
                                  │
                    ┌─────────────▼──────────────┐
                    │    PostgreSQL Database     │
                    │    (or configured DB)      │
                    └────────────────────────────┘
                                  │
                    ┌─────────────▼──────────────┐
                    │  Database Tables           │
                    │  • clients                 │
                    │  • contracts               │
                    │  • (relationships)         │
                    └────────────────────────────┘
```

---

## Data Flow

### Create Client Flow
```
User Interface
    │
    ├─ Fill Form
    ├─ Validate (client-side)
    │
    ▼
API Request
    │
    ├─ POST /api/clients
    ├─ JWT Token included
    │
    ▼
Backend Processing
    │
    ├─ Authenticate (JWT)
    ├─ Authorize (Role check)
    ├─ Validate DTO
    ├─ Check duplicate email
    ├─ Generate clientCode
    ├─ Save to database
    │
    ▼
API Response
    │
    ├─ 201 Created (success)
    ├─ Return Client object
    │
    ▼
Frontend Update
    │
    ├─ Invalidate React Query cache
    ├─ Show success toast
    ├─ Redirect to list/detail
    │
    ▼
User Sees Updated List
```

---

## Entity Relationships

```
┌──────────────────┐         1:N        ┌──────────────────┐
│     Clients      │◄──────────────────►│    Contracts     │
├──────────────────┤   (ClientId)       ├──────────────────┤
│ id (PK)          │                    │ id (PK)          │
│ clientCode       │                    │ contractCode     │
│ name             │                    │ clientId (FK)    │
│ email (Unique)   │                    │ siteId (FK)      │
│ type             │                    │ type             │
│ status           │                    │ frequency        │
│ ...              │                    │ status           │
│ createdAt        │                    │ ...              │
│ deletedAt        │                    │ deletedAt        │
└──────────────────┘                    └──────────────────┘
        ▲                                        ▲
        │            1:N                        │
        │       (ClientId)                      │
        │                            1:N (SiteId)
        │                                       │
        └────────────────────┐                  │
                             │                  │
                     ┌───────▼─────────┐        │
                     │      Sites      │        │
                     ├─────────────────┤        │
                     │ id (PK)         │        │
                     │ siteName        │        │
                     │ clientId (FK)   │◄───────┘
                     │ ...             │
                     │ deletedAt       │
                     └─────────────────┘
```

---

## API Endpoint Categories

```
┌──────────────────────────────────────────────────────────────┐
│                     REST API Endpoints                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  CREATE                │  READ                              │
│  ────────────────────  │  ─────────────────────             │
│  POST /clients         │  GET /clients                      │
│  POST /clients/batch   │  GET /clients/:id                  │
│  POST /contracts       │  GET /clients/search               │
│  POST /contracts/batch │  GET /contracts                    │
│                        │  GET /contracts/:id                │
│  UPDATE                │  GET /contracts/search             │
│  ──────────────────    │                                    │
│  PATCH /clients/:id    │  DELETE                            │
│  PATCH /clients/batch  │  ──────────────────                │
│  PATCH /contracts/:id  │  DELETE /clients/:id               │
│  PATCH /contracts/batch│  POST /clients/batch/delete        │
│                        │  DELETE /contracts/:id             │
│  RESTORE               │  POST /contracts/batch/delete      │
│  ──────────────────    │                                    │
│  POST /clients/:id/...│  RESTORE                           │
│  POST /contracts/:id/..│  ──────────────────                │
│                        │  POST /clients/:id/restore         │
│                        │  POST /contracts/:id/restore       │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## Request/Response Flow

### Successful Request
```
┌─────────────────────────┐
│   HTTP Request          │
│ ─────────────────────   │
│ POST /api/clients       │
│ Authorization: Bearer.. │
│                         │
│ {                       │
│   "name": "Test",       │
│   "email": "t@ex.com",  │
│   "type": "COMPANY"     │
│ }                       │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────┐
│   Server Processing     │
│ ─────────────────────   │
│ ✓ Authenticate         │
│ ✓ Authorize            │
│ ✓ Validate DTO         │
│ ✓ Check constraints    │
│ ✓ Generate code        │
│ ✓ Save to DB           │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────┐
│   HTTP Response         │
│ ─────────────────────   │
│ 201 Created             │
│                         │
│ {                       │
│   "id": "uuid",         │
│   "clientCode": "C..",  │
│   "name": "Test",       │
│   "email": "t@ex.com",  │
│   "status": "PROSPECT", │
│   "createdAt": "..."    │
│ }                       │
└─────────────────────────┘
```

### Error Request
```
┌─────────────────────────┐
│   HTTP Request          │
│ ─────────────────────   │
│ POST /api/clients       │
│                         │
│ {                       │
│   "email": "test"       │ ← Invalid email
│ }                       │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────┐
│   Validation Failed     │
│ ─────────────────────   │
│ ✗ Missing name          │
│ ✗ Invalid email format  │
│ ✗ Missing type          │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────┐
│   HTTP Response         │
│ ─────────────────────   │
│ 400 Bad Request         │
│                         │
│ {                       │
│   "statusCode": 400,    │
│   "message": "Please...",
│   "error": "BadRequest" │
│ }                       │
└─────────────────────────┘
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND                                 │
├─────────────────────────────────────────────────────────────┤
│ • React 18+              - UI Framework                     │
│ • TypeScript             - Type Safety                      │
│ • React Router           - Client-side Routing             │
│ • React Hook Form        - Form Management                 │
│ • Yup                    - Schema Validation               │
│ • React Query            - Data Caching/Sync              │
│ • Axios                  - HTTP Client                     │
│ • Tailwind CSS           - Styling                         │
│ • i18n (react-i18next)   - Internationalization           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    BACKEND                                  │
├─────────────────────────────────────────────────────────────┤
│ • NestJS                 - Framework                       │
│ • TypeScript             - Type Safety                     │
│ • TypeORM                - Database ORM                    │
│ • PostgreSQL             - Database (configurable)         │
│ • JWT                    - Authentication                  │
│ • Class Validator        - DTO Validation                 │
│ • Passport               - Auth Strategy                   │
│ • Multer                 - File Upload (optional)          │
└─────────────────────────────────────────────────────────────┘
```

---

**Last Updated**: January 17, 2026  
**Architecture Version**: 1.0
